system_info:
  default_user:
    name: cloud-user
    lock_passwd: false
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    groups: [wheel, adm, systemd-journal]
    shell: /bin/bash
    
disable_root: false
ssh_pwauth: true